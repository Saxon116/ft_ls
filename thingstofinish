-l not that aligned in collunm
(OP) macro
sortie d'erreur pour les messages d'erreurs
couleurs (FIFO and UNKNOWN missing)
norme
ACL attributes
-u -g -d -f


minor(lstat->st_rdev)
major(lstat->st_rdev)
